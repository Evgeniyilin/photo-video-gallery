import React from 'react';
import s from './Header.module.css';

const Header = () => {
    return (
        <div className={s.header}>
            PHOTO VIDEO GALLERY
        </div>
    );
};

export default Header;